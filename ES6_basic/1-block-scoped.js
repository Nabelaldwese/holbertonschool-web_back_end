export default function taskBlock(trueOrFalse) {
  let task = false;
  let task2 = true;

  if (trueOrFalse) {
    const taskBlock = true;
    const taskBlock2 = false;
  }

  return [task, task2];
}
